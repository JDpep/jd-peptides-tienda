[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -ra
    --strict-markers
    --tb=short
markers =
    slow: tests que demoran > 1s
    integration: tests que dependen de servicios externos (skipear en CI rápido)
